//src/components/layout/content/Details.jsx
import "../../../styles/layout/details/Details.css"

const Details = ( {message} ) => {
    return (
        <h1>{message}</h1>
    );
};

export default Details;